# Lumin platform monorepo – just recipes
# Usage: just [recipe]
#
# This is the ENTRY template. Developers fetch it as `remote.just` (see `just fetch`),
# which then imports the module files below. `just fetch` downloads every module it
# imports, so adding a new `import?` line here is enough — no local justfile changes.

import? 'core.just'
import? 'git.just'
import? 'review.just'
import? 'linear.just'

default:
    just --list
